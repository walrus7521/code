TODO: (java)
====
sorting: http://www.studytonight.com/data-structures/bubble-sort
    [x] Bubble Sort.    : each largest element bubbles down to end
    [x] Insertion Sort. : puts item in correct location each iteration
    [x] Selection Sort. : find smallest and exchg with first position
    [ ] Quick Sort.     : 
    [ ] Merge Sort.     : 
    [ ] Heap Sort.      : 

arrays: zip 
search: bin, boyer
avl: mechanics

http://dogsaholic.com/care/how-to-make-a-dog-wheelchair.html

practice:
    list reverse
    palindrome
    anagram
    bfs -> tree or graph
    heap
    ring

Graph algorithms:
    Breadth First Search
    Depth First Search
    Shortest Path from source to all vertices - Dijkstra
    Shortest Path from every vertex to every other vertex - Floyd Warshall
    Minimum Spanning tree - Prim
    Minimum Spanning tree - Kruskal
    Topological Sort


Coolness:
https://elixir.free-electrons.com/linux/latest/source/drivers/base/regmap/regcache-rbtree.c
embed an rb_root inside a struct

struct dude_t {
    struct page *page;
    struct rb_node node;
    struct list_head remove_node;
};

struct rb_node **new = NULL
new = &ring->a_dude.rb_node;
struct dude_t *this = container_of(*new, struct dude_t, node);

/**
 * container_of - cast a member of a structure out to the containing structure
 * @ptr:    the pointer to the member.
 * @type:   the type of the container struct this is embedded in.
 * @member: the name of the member within the struct.
 *
 */
#define container_of(ptr, type, member) ({              \
    void *__mptr = (void *)(ptr);                   \
    BUILD_BUG_ON_MSG(!__same_type(*(ptr), ((type *)0)->member) &&   \
             !__same_type(*(ptr), void),            \
             "pointer type mismatch in container_of()");    \
    ((type *)(__mptr - offsetof(type, member))); })

// lots of cool stuff
http://elixir.free-electrons.com/linux/latest/source/include/linux/kernel.h
