

chips:
pc <- usb -> mcp2210 <- spi -> mcp25625 <- CAN -> autopilot
buspirate -><- i2c - sc16is750 - rs485 -><- sc18is700 - i2c -> mag
arduino <-> testing chips and algorithms

SC16IS750 is the i2c to UART
SC18IS700 is UART to i2c

GYRO: L3GD20H, spark[yes], 16-bit counts X,Y,Z
    raw_body_rates
    gyros.samples

MAG: HMC5883L, spark[yes], 16-bit counts X,Y,Z
    sum x,y,z values and average, normalize vector
    Sensor_Mgmt_Get_Magnetic_Vector

ACCEL: MMA8451Q, spark[yes], 16-bit counts X,Y,Z (400 hz), body accelerations
    accels.samples
    filter, Sensor_Mgmt_Get_Acceleration_Vector

ADAHRS:
    ADAHRS_Set_Measured_Angular_Rates(&state.adahrs, &state.raw_body_rates);
    ADAHRS_Set_Measured_Magnetic_Vector(&state.adahrs, &outputs.mag_vector);
    ADAHRS_Set_Measured_G_Forces(&state.adahrs, &inputs.median_accels);
    
State tables: 

Algos:


