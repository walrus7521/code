
   0. Design Patterns
https://en.wikibooks.org/wiki/C%2B%2B_Programming/Code/Design_Patterns

   1. Basics
    function overloading
    initialization: int x = 1; & int x {1}; 
                    vector<int> v {1,2,3,4,5,6};
                    vector<int> v = {1,2,3,4,5,6};
                    auto b = true; // type deduction
    const : semantic for immutability - eval'd at runtime
    constexpr : constans eval'd at compile time (instead of #define)
    range for: for (auto x : {1,2,3,4}) cout << x;
    no copy : sort(vector<int>& v){} or sum(const vector<int>& v){} 


   2. UDT
    struct, class, union, enum (enum class is strongly typed, can be used in switch)


=> 3. Modular

   4. Class

   5. Templates

   6. Libs

   7. Strs & RegEx

   8. Streams

   9. Containers

  10. Algorithm

  11. Utils

  12. Numerics

  13. Concurrency

